@model Interfaces.Magazyn.Dtos.PozycjaInwentaryzacjiDeleteDto

@{
    ViewData["Title"] = "Usuń pozycje inwentaryzacji";
    var p = Model.Pozycja;
    var jm = p.Produkt?.DomyslnaJednostka?.Kod ?? "j.m.";
    var docStatus = p.Dokument?.Status ?? "-";
    var docStatusClass = string.Equals(docStatus, "Draft", StringComparison.OrdinalIgnoreCase)
        ? "entity-badge--warning"
        : string.Equals(docStatus, "Closed", StringComparison.OrdinalIgnoreCase)
            ? "entity-badge--success"
            : "entity-badge--muted";
    var roznica = p.IloscPoliczona - p.IloscOczekiwana;
}

<div class="news-admin-page">
    <section class="news-admin-header news-admin-header--danger">
        <div class="news-admin-header__title">
            <span class="news-admin-header__icon news-admin-header__icon--danger"><i class="bi bi-trash3"></i></span>
            <div>
                <h1 class="news-admin-header__heading mb-1">Usuń pozycję inwentaryzacji</h1>
                <p class="news-admin-header__subheading mb-0">Potwierdź usunięcie pozycji z dokumentu inwentaryzacji.</p>
            </div>
        </div>
    </section>

    <section class="news-delete-panel">
        <div class="news-delete-warning" role="alert">
            <i class="bi bi-exclamation-triangle-fill"></i>
            <div>
                <strong>Usunięcie pozycji inwentaryzacji</strong>
                <div>@(Model.CzyDokumentDraft ? "Usunięcie pozycji zmieni podsumowanie dokumentu inwentaryzacji." : (Model.PowodBlokady ?? "Operacja może być zablokowana."))</div>
            </div>
        </div>

        @if (!ViewData.ModelState.IsValid)
        {
            <div class="alert alert-danger mt-3"><div asp-validation-summary="All" class="mb-0"></div></div>
        }

        <div class="news-details-grid">
            <div class="news-details-item"><div class="news-details-item__label">Dokument inwentaryzacji</div><div class="news-details-item__value entity-mono">@(p.Dokument?.Numer ?? "-")</div></div>
            <div class="news-details-item"><div class="news-details-item__label">Status dokumentu</div><div class="news-details-item__value"><span class="entity-badge @docStatusClass">@docStatus</span></div></div>
            <div class="news-details-item"><div class="news-details-item__label">Lp</div><div class="news-details-item__value">@p.Lp</div></div>
            <div class="news-details-item"><div class="news-details-item__label">Produkt</div><div class="news-details-item__value">@(p.Produkt?.Kod ?? "-") - @(p.Produkt?.Nazwa ?? "-")</div></div>
            <div class="news-details-item"><div class="news-details-item__label">Lokacja</div><div class="news-details-item__value">@(p.Lokacja?.Kod ?? "-") / @(p.Lokacja?.Magazyn?.Nazwa ?? "-")</div></div>
            <div class="news-details-item"><div class="news-details-item__label">Ilość oczekiwana</div><div class="news-details-item__value">@p.IloscOczekiwana.ToString("0.###") @jm</div></div>
            <div class="news-details-item"><div class="news-details-item__label">Ilość policzona</div><div class="news-details-item__value">@p.IloscPoliczona.ToString("0.###") @jm</div></div>
            <div class="news-details-item"><div class="news-details-item__label">Różnica</div><div class="news-details-item__value">@roznica.ToString("+0.###;-0.###;0") @jm</div></div>
        </div>

        <form asp-action="Delete" method="post" class="news-form-actions">
            <input type="hidden" name="id" value="@p.Id" />
            <button type="submit" class="btn btn-danger news-form-actions__primary" @(Model.CzyMoznaUsunac ? null : "disabled")><i class="bi bi-trash3-fill"></i> Usuń pozycję inwentaryzacji</button>
            <a asp-action="Details" asp-route-id="@p.Id" class="btn btn-outline-info"><i class="bi bi-eye"></i> Szczegóły</a>
            <a asp-action="Index" class="btn btn-outline-secondary"><i class="bi bi-arrow-left"></i> Anuluj</a>
        </form>
    </section>
</div>
